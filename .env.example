# Copy this file to `.env` and fill in real values.
# NEVER commit `.env` (it's .gitignored and .dockerignored).
# NEVER commit real tokens, only this example template.

# LLM provider for the agent loop
FREELLM_API_KEY=sk-fake-0000000000000000000000000000

# GitHub token used by `gh` (fine-grained PAT with repo + PR scopes)
GITHUB_TOKEN=ghp_faketoken000000000000000000000
